{
  "description": "Check for jdtls installation on session start",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/check-jdtls.sh",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
